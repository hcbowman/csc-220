#ifndef MATRIX_HPP
#define MATRIX_HPP

#include<iostream>


class Matrix {

  private:

    int rows, columns;

	public:

		Matrix(int r, int c) {
      rows = r;
      columns = c;
		}
    ~Matrix();

    print
    add
    scalar_multiplcation
    transpose


    + (addition)
    * (scalar multiplication)
    << (stream injection)


};

#endif
